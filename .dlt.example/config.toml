[destination.bronze]
bucket_url = "s3://churn-data-lake"

[destination.bronze.credentials]
aws_access_key_id = "admin"
aws_secret_access_key = "password"
endpoint_url = "http://minio:9000"
aws_region = "us-east-1"